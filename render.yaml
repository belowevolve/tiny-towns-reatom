services:
  - type: web
    name: tiny-towns-ws
    runtime: docker
    dockerfilePath: server/Dockerfile
    dockerContext: server
    plan: free
    envVars:
      - key: PORT
        value: "10000"
